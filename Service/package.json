{
  "name": "gcms-dbm-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon ./bin/www",
    "test": "mocha --timeout 50000 --recursive --watch",
    "coverage": "nyc --reporter=html --reporter=text mocha --timeout 10000 --recursive --exit"
  },
  "dependencies": {
    "config": "3.1.0",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^16.4.5",
    "express": "~4.16.1",
    "lodash": "4.17.15",
    "mysql": "^2.18.1",
    "node-cron": "^3.0.0",
    "nodemon": "^1.19.4",
    "nyc": "^15.1.0",
    "oracledb": "^6.4.0"
  },
  "devDependencies": {
    "eslint": "^6.5.1",
    "mocha": "^6.2.2"
  }
}
